{{ivy-codemirror
    value=value
    readonly=readonly
    name=name
    class=class
    options=options
    valueUpdated=(action onkeyup)
}}
{{#power-select
  onchange=(action onchange)
  selected=mode
  options=modes as |mode|
}}
  {{mode.name}}
{{/power-select}}
